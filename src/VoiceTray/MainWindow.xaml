<Window x:Class="VoiceTray.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VoiceTray"
        Width="620"
        Height="420"
        Icon="Assets/VoiceTray.ico"
        MinWidth="460"
        MinHeight="320"
        Closing="OnClosing">
    <Window.Resources>
        <Style x:Key="RecordingIndicatorStyle" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed" />
            <Setter Property="Opacity" Value="0" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                    <Setter Property="Opacity" Value="1" />
                    <DataTrigger.EnterActions>
                        <BeginStoryboard x:Name="RecordingPulseStoryboard">
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="0.62"
                                                 Duration="0:0:0.55" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                 To="1.04"
                                                 Duration="0:0:0.55" />
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                 To="1.04"
                                                 Duration="0:0:0.55" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="RecordingPulseStoryboard" />
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RecordingDotStyle" TargetType="Ellipse">
            <Setter Property="Fill" Value="#FF3B30" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Height" Value="10" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="{Binding StatusText, StringFormat=Status: {0}}"
                       TextTrimming="CharacterEllipsis" />

            <Border Grid.Column="1"
                    Padding="10,5"
                    Background="#1FFF3B30"
                    BorderBrush="#55FF3B30"
                    BorderThickness="1"
                    CornerRadius="14"
                    Style="{StaticResource RecordingIndicatorStyle}">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Style="{StaticResource RecordingDotStyle}" />
                    <Path Width="10"
                          Height="14"
                          Margin="0,0,6,0"
                          Stretch="Uniform"
                          Fill="#FF3B30"
                          Data="M5,0 C2.8,0 1,1.8 1,4 L1,7 C1,9.2 2.8,11 5,11 C7.2,11 9,9.2 9,7 L9,4 C9,1.8 7.2,0 5,0 Z M0,6.6 L0,7 C0,9.6 1.9,11.8 4.4,12.2 L4.4,14 L5.6,14 L5.6,12.2 C8.1,11.8 10,9.6 10,7 L10,6.6 L8.8,6.6 L8.8,7 C8.8,9.1 7.1,10.8 5,10.8 C2.9,10.8 1.2,9.1 1.2,7 L1.2,6.6 Z" />
                    <TextBlock VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Foreground="#C62828"
                               Text="Recording" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="1"
                    Margin="0,0,0,12"
                    Orientation="Horizontal">
            <Button MinWidth="76"
                    Margin="0,0,8,0"
                    Command="{Binding StartCommand}"
                    Content="Start" />
            <Button MinWidth="76"
                    Margin="0,0,8,0"
                    Command="{Binding StopCommand}"
                    Content="Stop" />
            <Button MinWidth="76"
                    Margin="0,0,8,0"
                    Command="{Binding CopyCommand}"
                    Content="Copy" />
            <Button MinWidth="76"
                    Margin="0,0,8,0"
                    Command="{Binding PasteCommand}"
                    Content="Paste" />
            <Button MinWidth="76"
                    Command="{Binding ClearCommand}"
                    Content="Clear" />
        </StackPanel>

        <TextBox x:Name="MemoTextBox"
                 Grid.Row="2"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 FontFamily="Consolas"
                 FontSize="14"
                 HorizontalScrollBarVisibility="Auto"
                 SpellCheck.IsEnabled="False"
                 Text="{Binding MemoText, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
